# =============================================================================
# PMO Tool Environment Configuration
# =============================================================================
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration (Required)
# -----------------------------------------------------------------------------
# Get these from your Supabase project settings: Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# -----------------------------------------------------------------------------
# LLM API Keys (Required)
# -----------------------------------------------------------------------------
# Google AI Studio: https://aistudio.google.com/app/apikey
GOOGLE_GENERATIVE_AI_API_KEY=your-google-ai-api-key

# OpenAI: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key

# -----------------------------------------------------------------------------
# LLM Model Configuration (Optional - defaults shown)
# -----------------------------------------------------------------------------
# Override these to use different model versions
# LLM_PRIMARY_MODEL=gemini-3-pro-preview
# LLM_FALLBACK_MODEL=gpt-5.2
# LLM_UTILITY_MODEL=gemini-2.0-flash

# -----------------------------------------------------------------------------
# Google Calendar Integration (Optional)
# -----------------------------------------------------------------------------
# To enable Google Calendar integration:
# 1. Go to https://console.cloud.google.com
# 2. Create a new project or select existing
# 3. Enable the Google Calendar API (APIs & Services > Library)
# 4. Go to "Credentials" and create OAuth 2.0 Client ID (Web application)
# 5. Add authorized redirect URI (see below)
# 6. Copy the Client ID and Client Secret

GOOGLE_CALENDAR_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CALENDAR_CLIENT_SECRET=your-client-secret
GOOGLE_CALENDAR_REDIRECT_URI=http://localhost:3000/api/google/calendar/callback

# -----------------------------------------------------------------------------
# Google Drive Auto-Ingestion (Optional)
# -----------------------------------------------------------------------------
# To enable automatic transcript import from Google Drive:
# 1. Enable the Google Drive API in the same Google Cloud project
# 2. Add authorized redirect URI (see below)
# 3. You can reuse the same OAuth credentials as Calendar

GOOGLE_DRIVE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_DRIVE_CLIENT_SECRET=your-client-secret
GOOGLE_DRIVE_REDIRECT_URI=http://localhost:3000/api/google/drive/callback

# Security secrets for webhooks and cron jobs
# Generate with: openssl rand -hex 16
WEBHOOK_SECRET=your-random-webhook-secret
CRON_SECRET=your-random-cron-secret

# Public URL for webhook registration (required for Drive webhooks)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Logging Configuration (Optional)
# -----------------------------------------------------------------------------
# Set to 'debug' for verbose output in development
# Defaults to 'info' in production, 'debug' in development
# LOG_LEVEL=debug

# -----------------------------------------------------------------------------
# Production Redirect URIs
# -----------------------------------------------------------------------------
# For production, update the redirect URIs to your domain:
# GOOGLE_CALENDAR_REDIRECT_URI=https://your-domain.com/api/google/calendar/callback
# GOOGLE_DRIVE_REDIRECT_URI=https://your-domain.com/api/google/drive/callback
# NEXT_PUBLIC_APP_URL=https://your-domain.com
